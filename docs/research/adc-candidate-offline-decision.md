# Whole-sequence ADC candidate: offline decision

The stock snapshot selects the short static path: signed sample rate zero takes
the Stary bypass branch, giving a predicted 97 writes, two readbacks, and 25,200 µs
of requested sleeps. Nine raw captures and their event-side byte checks are
available. The next useful work is whole-sequence instantiation from those bytes.

The frozen full verifier's index rejection remains a negative experiment result.
It concerns eight copied static contracts omitted by the runner's nonrecursive
index, not missing raw captures. All eight are present and match the frozen
checker's source pins. A separate offline completeness review can state exactly
what is supported without editing the old index or relabeling its rejection.
Future indexing needs a control that invokes the actual runner index function.

The admitted batch therefore contains evidence review and prospective indexing,
typed candidate instantiation, a read-only capacity review, and a decision gate.
No guest runs in this batch. The capacity review preserves existing files and the
2 GiB preboot threshold; any reversible archival action needs its own admitted
restoration contract before implementation.

Once the candidate is complete, the preferred runtime question is whether stock
code follows its entire predicted sequence and reaches a post-routine checkpoint,
stopping on divergence. The two read values remain explicitly unobserved hardware
inputs. Their known local bit-16 protocol and logging use justify considering a
bounded synthetic hypothesis, not claims about FPGA state or acquisition.
